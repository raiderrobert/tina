# Examples

| Path | What it is |
|------|------------|
| [`tina.toml`](tina.toml) | A key reference: two sources, two harnesses, the `cloudrun` executor. Every configurable surface in one file, not a working setup. |
| [`bug-triage/`](bug-triage/) | One complete track — config, skill and a walkthrough. This is the track the README recording runs, and `demo/record.sh` runs these exact files. |

Two configs, because they answer different questions: `tina.toml` shows what can
be configured, `bug-triage/` shows one thing that actually runs.
